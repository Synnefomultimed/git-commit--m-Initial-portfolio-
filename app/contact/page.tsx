import { ArrowUpRight, Mail, Phone } from "lucide-react"
import { site } from "@/lib/site"

export default function ContactPage() {
  return <main className="px-4 pb-24 pt-32 md:px-6 md:pt-40"><div className="mx-auto max-w-5xl"><p className="text-xs font-semibold uppercase tracking-[0.3em] text-gold">Contact</p><h1 className="mt-4 max-w-4xl font-heading text-5xl font-semibold tracking-tight md:text-7xl">Let’s create something worth showing.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-muted-foreground">For design projects, freelance work, packaging, social media campaigns, branding, or other creative requirements, get in touch.</p><div className="mt-12 grid gap-4 sm:grid-cols-2">{site.phone && <a href={`tel:${site.phone}`} className="glass rounded-3xl p-6 transition hover:border-gold/30"><Phone className="h-6 w-6 text-gold"/><p className="mt-5 text-xs uppercase tracking-widest text-muted-foreground">Phone</p><p className="mt-1 font-heading text-lg font-semibold">{site.phone}</p></a>}{site.email && <a href={`mailto:${site.email}`} className="glass rounded-3xl p-6 transition hover:border-gold/30"><Mail className="h-6 w-6 text-gold"/><p className="mt-5 text-xs uppercase tracking-widest text-muted-foreground">Email</p><p className="mt-1 font-heading text-lg font-semibold">{site.email}</p></a>}<a href={site.behance} target="_blank" rel="noreferrer" className="glass rounded-3xl p-6 transition hover:border-gold/30 sm:col-span-2"><ArrowUpRight className="h-6 w-6 text-gold"/><p className="mt-5 text-xs uppercase tracking-widest text-muted-foreground">Portfolio</p><p className="mt-1 font-heading text-lg font-semibold">View Behance</p></a></div></div></main>
}
